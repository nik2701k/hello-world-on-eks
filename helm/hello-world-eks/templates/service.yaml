apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-world-eks.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "hello-world-eks.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hello-world-eks.selectorLabels" . | nindent 4 }}
